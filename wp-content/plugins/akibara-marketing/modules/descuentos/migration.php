<?php
/**
 * Descuentos — placeholder de migracion.
 *
 * Requerido por module.php (require_once). El archivo debe existir aunque
 * no tenga logica activa. Originalmente reservado para v11.1 que nunca llego;
 * se mantiene como noop para no romper la cadena de requires del modulo.
 *
 * Lifted from server-snapshot/.../modules/descuentos/migration.php (unchanged logic).
 * Load guard not needed — no definitions here, pure noop.
 *
 * @package Akibara\Marketing\Descuentos
 */

defined( 'ABSPATH' ) || exit;
